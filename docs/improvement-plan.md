# План усиления — аудит и порядок работ

> Аудит от 01.09.2026 против коммита `8d2b94c`. Метод: одиннадцать параллельных срезов (безопасность · корректность · конкурентность · архитектура · производительность · тесты · фронтенд · данные и миграции · эксплуатация · продукт · четыре фронта от критика полноты), затем адверсарная проверка каждой находки по исходникам. 108 находок прошли проверку, часть отвергнута. Ключевые утверждения — `SEC-METRICS`, `SEC-IDOR`, `JOB-RETRY`, `FE-CHAT-CSRF`, `COST-GAP-CALLS`, `LEGAL-LICENSE` — перепроверены вручную по коду.

**44 задачи · 3 критических · 22 высоких · 21 быстрая победа · 4 волны**

> **Обновление 25.09.2026.** Волны 1–4 влиты в main, но последующий мерж админ-панели откатил часть работы. Фактический статус всех 44 задач и волна 5, закрывающая регрессии, — в [§8](#8-статус-на-25092026-и-волна-5).

Парные документы: [`deep-research-roadmap.md`](./deep-research-roadmap.md) (продуктовые фазы P0–P4), [`execution-plan.md`](./execution-plan.md) (что уже сделано), [`frontend-roadmap.md`](./frontend-roadmap.md), [`eval-harness.md`](./eval-harness.md).

---

## 1. Вердикт

Система собрана серьёзно: сложные, легко ломаемые места сделаны лучше, чем в среднем продакшене. Настоящий `SELECT … FOR UPDATE SKIP LOCKED` с нагрузочным тестом на восьми потоках, частичный уникальный индекс под CAS финализации, SSRF-защита с ре-валидацией на каждом хопе и пиннингом IP, корректный самописный HS256, digest-пиннинг образов, one-shot `migrate`-сервис, `prepare_threshold: None` под PgBouncer. Слои разделены не на словах: ниже `src/api/` нет ни одного импорта веб-фреймворка.

Тормозит проект три вещи.

**Первое: ничего не проверяется.** 516 тестов, гейт дрейфа Alembic, eval-гейт и `vue-tsc` существуют — и CI не запускает ни одного из них: в `.github/workflows/postgres-smoke.yml` ровно три шага, и `pytest` среди них нет. Именно поэтому две другие проблемы дожили до сегодня.

**Второе: «починили здесь, забыли в близнеце».** Re-dispatch в брокер описан, закомментирован и покрыт тестом для восстановления зависших задач — и отсутствует в ветке повторной попытки после ошибки. Любой транзиентный сбой поиска вешает исследование навсегда. Аналогично `merge_research_graph_state` получил блокировку строки, а остальные писатели по-прежнему делают незащищённый read-modify-write.

**Третье: нумерация `[Sn]` выводится независимо в пяти местах из четырёх разных пулов источников.** Панель конфликтов, вкладка уверенности и чат ссылаются на номера, означающие не то же самое, что в отчёте перед глазами пользователя. Ломается ровно то, на чём построено позиционирование, — проверяемость.

Две вещи требуют внимания раньше всего остального: неаутентифицированный `/metrics` помечает каждый запрос сырым путём и тем самым публикует все токены публичных ссылок любому, кто сделает `curl` на 8501; и семейство маршрутов `/v1/tasks` + `/v1/search-jobs` несёт только «залогинен», заново открывая IDOR, который `execution-plan.md:45` считает закрытым.

---

## 2. Что уже сделано хорошо

Это не трогаем — это фундамент, на котором дешевеет всё остальное.

| Что | Где |
|---|---|
| **Захват задач — настоящая конкурентность.** `.with_for_update(skip_locked=True)`, CAS-переходы с проверкой rowcount, партиальный уникальный индекс `uq_running_finalize_job_per_research`, регрессионный тест на 8 потоков против живого Postgres | `sqlalchemy_task_store.py:490,693` · `20260622_000017` |
| **SSRF-защита сильнее типовой.** Ручная ре-валидация на каждом редиректе, нормализация IPv4-mapped IPv6 (`::ffff:169.254.169.254` ловится), `safe_post_json` пиннит проверенный IP, сохраняя Host/SNI — TOCTOU DNS-rebind закрыт | `src/net_safety.py` |
| **Самописный JWT корректен там, где это важно.** Подпись проверяется через `hmac.compare_digest` до разбора заголовка, `alg`/`typ` зафиксированы, `exp` обязателен, bootstrap падает на дефолтном или коротком ключе | `src/auth/security.py` · `src/bootstrap.py:58` |
| **Авторизация на уровне маршрута, единообразно.** ~30 роутов `/v1/research/{id}/*` на одной зависимости; `_ensure_research_access` сознательно отказывает legacy-строкам с NULL-владельцем | `src/api/app.py` · `research_service.py:505-515` |
| **Две реализации TaskStore в паритете 61 метода.** Именно это позволяет гонять 474 теста без Postgres | `src/repositories/` |
| **Контроль стоимости заложен в дизайн.** Профили глубины с бюджетом символов по качеству, кросс-процессный Redis-семафор на LLM, TTL-кэш поиска, корректно отказывающийся кэшировать пустой результат, `selectinload` на каждом read-пути | `search_depth_profiles.py` · `providers/rate_limit.py` |
| **Слой доверия глубже, чем у коммерческих аналогов.** Аудит цитат с кросс-языковым фолбэком по якорям, кластеризация независимости источников, проверка отзывов публикаций, композиция уверенности с раскрытием веса компонент — детерминированный код, обёрнутый так, что сбой не ломает финализацию | `src/agents/` · `trust_report_mixin.py` |
| **Операционные основы на месте.** Digest-пиннинг образов, one-shot `migrate` убирает гонку трёх воркеров, настоящий SIGTERM с согласованным `stop_grace_period`, `/health` с проверкой зависимостей, JSON-логи с correlation id | `docker-compose.yml` · `src/observability/` |
| **Данные: время и каскады сделаны правильно.** Все метки — `DateTime(timezone=True)` с aware-дефолтами, на Postgres-пути ни одного сравнения naive/aware; `ON DELETE CASCADE` на каждом FK с `passive_deletes=True`; у всех 18 миграций есть downgrade; NOT NULL-колонки добавляются в два шага; `graph_trail` ограничен и по времени, и по длине | `src/db/models.py` · `alembic/versions/` · `core/graph_history.py` |

---

## 3. Порядок работ

| Волна | Цель | Задач |
|---|---|---|
| **1. Заткнуть утечки и включить гейт** | Закрыть обе критические дыры, которые живы прямо сейчас, заставить CI реально проверять уже написанные 516 тестов и внести правки, которые прекращают жечь деньги и вешать исследования. Ничего ни от чего не зависит, каждый пункт откатывается отдельно. | 15 (14 — S) |
| **2. Закрыть доступ, приём и целостность задач** | Убрать межпользовательский доступ и захват аккаунта, сделать двойной запуск финализации невозможным, поставить реальный потолок на траты одного аккаунта. После этой волны систему можно показывать не только владельцу. | 11 |
| **3. Сделать слой доверия действительно доверенным** | `[Sn]` означает одно и то же везде, показанный конфликт — настоящий конфликт, у русских источников есть детектирование противоречий, первоисточники читаются. Здесь же conformance-сьют для сторов. | 7 |
| **4. Ёмкость, видимость, долговечность** | Поднять потолки (тредпул SSE, потерянные обновления `graph_state`, рост таблиц), дать оператору дашборды и алерты, закрыть историю с ошибками во фронте и — уже под защитой CI — выплатить структурный долг. | 11 |

### Критерии готовности

**Волна 1.** Токен шаринга не появляется в `/metrics`, а сам `/metrics` недоступен снаружи; pytest, `vue-tsc`, vitest и eval-гейт идут на каждом PR как required-проверки; упавшая один раз поисковая задача повторяется и исследование доходит до конца; отмена останавливает трату в пределах одного шага графа; grounded-чат работает на голом `docker compose up`; EASY/MEDIUM не делают ни одного выброшенного gap-вызова; early-stop экстракции срабатывает на HARD; один воркер держит один engine за свою жизнь; в корне есть LICENSE.

**Волна 2.** Пользователь B получает 404 на каждую задачу и джобу пользователя A; смена пароля требует текущего и рвёт все прочие сессии; финализация, пережившая таймаут, но делающая прогресс, не пере-диспатчится, а запись вытесненного раннера отсекается; три параллельных HARD с replan доходят до конца без затыка; лимиты держатся под 10 одновременными стартами и на пути plan-first/approve; `/v1/decompose` не создаёт джоб, LLM-роуты throttled per-user, `mas_llm_cost_usd_total` растёт; деплой без ключа рапортует unhealthy; учение по восстановлению из бэкапа пройдено.

**Волна 3.** На HARD-прогоне с replan каждый id источника в панели конфликтов, во вкладке проверки, в аудите цитат и в ответе чата ведёт на тот же URL, что и соответствующий `[Sn]` в отчёте; на курируемом наборе адъюдикация оставляет настоящие противоречия и отсекает большинство ложных; русская пара с отрицанием детектируется, английские `reason` не попадают в русский отчёт; PDF-источник отдаёт извлечённый текст; все методы протокола прогнаны против обоих бэкендов.

**Волна 4.** 100 одновременных SSE не роняют отзывчивость `/health`; тест на двух писателей не теряет ни одного ключа `graph_state` за 100 итераций, возобновлённый прогон даёт ту же последовательность шагов, что и свежий; Grafana поднимается с заполненными дашбордами, все три воркера скрейпятся, убийство воркера поднимает алерт; retention удаляет терминальные исследования по расписанию; ни одна вьюха не показывает сырую строку ошибки, 401 уводит на логин; ruff и coverage идут в CI; у кулдаунов доменов есть тесты; удаление аккаунта каскадирует и отзывает токены шаринга.

---

## 4. Сводная таблица

| ID | Задача | Сер. | Объём | Волна |
|---|---|---|---|---|
| `SEC-METRICS` | Метка метрик — шаблон маршрута, `/metrics` не наружу | крит | S | 1 |
| `SEC-IDOR` | Скоупинг по владельцу для `/v1/tasks` и `/v1/search-jobs` | крит | M | 2 |
| `SEC-ACCOUNT` | Текущий пароль при смене, отзываемые сессии, запрет тихого слияния OAuth | выс | M | 2 |
| `SEC-LOGIN-LIMITER` | Пробросить реальный IP клиента | выс | S | 1 |
| `SEC-ERROR-LEAK` | Убрать три обёртки `except Exception → HTTPException(500, str(e))` | ср | S | 1 |
| `CI-PYTEST` | Гонять бэкенд-сьют на каждом PR | выс | S | 1 |
| `CI-WEB` | Job для фронта: `vue-tsc` и vitest | выс | S | 1 |
| `CI-EVAL-SMOKE` | Подключить eval-гейт, убрать хардкод порта в смоуке | ср | S | 1 |
| `CI-TOOLING` | pyproject.toml, ruff, coverage, снять wildcard-импорты | ср | M | 4 |
| `TEST-STORE-CONFORMANCE` | Один conformance-сьют против обеих реализаций TaskStore | выс | L | 3 |
| `JOB-RETRY` | Возвращать повторяемые задачи в Redis | выс | S | 1 |
| `JOB-LEASE` | Продлевать аренду финализации, отсекать вытесненный раннер | крит | M | 2 |
| `REPORT-CONFLICTS-HEADING` | Перестать выкидывать раздел «Конфликты» из отчёта | выс | S | 1 |
| `JOB-CANCEL` | Уважать отмену внутри графа финализации | выс | S | 1 |
| `JOB-REPLAN-DEADLOCK` | Финализирующие воркеры не должны ждать сами себя | выс | M | 2 |
| `STATE-LOCKED-MERGE` | Все записи `graph_state` — через merge под блокировкой | ср | M | 4 |
| `GRAPH-TOPOLOGY` | Свести три кодировки графа финализации к одной | ср | M | 4 |
| `CITE-POOL` | Один канонический список источников для всех потребителей | выс | M | 3 |
| `CITE-CHAT` | Чат цитирует нумерацию отчёта и видит отчёт целиком | выс | M | 3 |
| `CONFLICT-ADJUDICATE` | Подтверждать кандидатов в конфликты LLM перед показом | выс | M | 3 |
| `CONFLICT-LEXICONS` | Языковые словари конфликтов и доказательств | выс | M | 3 |
| `COST-GAP-CALLS` | Проверять условие ветвления до вызова gap-анализа | выс | S | 1 |
| `COST-EARLY-STOP` | Сделать ранний останов экстракции достижимым | выс | S | 1 |
| `PERF-WORKER-LOOP` | Строить сервис один раз, не сканировать `researches` каждый цикл | выс | S | 1 |
| `PERF-SSE` | Перевести SSE на async | выс | M | 4 |
| `ADMIT-ATOMIC` | Атомарный приём на каждом входе в тратящее состояние | выс | M | 2 |
| `ABUSE-ROUTES` | Персональные лимиты, учёт трат, `/v1/decompose` без сиротских джоб | выс | M | 2 |
| `PERF-DB` | Индексы на JSONB-выборки, не тянуть блобы отчётов в списки | ср | S | 2 |
| `COST-DDG` | DuckDuckGo не должен ждать два проигравших запроса | ср | S | 1 |
| `OPS-OBSERVABILITY` | Дашборды, правила алертов, скрейп воркеров | выс | M | 4 |
| `OPS-BOOT-HEALTH` | Деплой без рабочего LLM не должен рапортовать healthy | ср | S | 2 |
| `OPS-BACKUP` | Путь бэкапа и восстановления | ср | S | 2 |
| `OPS-RETENTION` | Окно хранения исследований и скачанных тел страниц | ср | M | 4 |
| `FE-CHAT-CSRF` | Заголовки авторизации и CSRF из клиента стрима чата | выс | S | 1 |
| `FE-FAILURES` | Показывать ошибки экспорта, гарантировать терминальное событие | ср | S | 4 |
| `FE-ERRORS` | Типизированные ошибки API, локализация, восстановление на 401 | ср | M | 4 |
| `ACQ-PDF` | Читать PDF, а не кормить мохибейк HTML-экстрактору | выс | M | 3 |
| `ACQ-COOLDOWN` | Один таймаут не должен навсегда снижать терпимость к домену | ср | M | 4 |
| `LANG-SINGLE-SOURCE` | Определять язык один раз, удалить пять детекторов | ср | M | 3 |
| `LEGAL-LICENSE` | Добавить LICENSE | ср | S | 1 |
| `DATA-LIFECYCLE` | Удаление аккаунта, FK на владельца, жизненный цикл ссылок | ср | M | 4 |
| `DATA-INDEX-QUEUE` | Индексы под захват задач, sweep и cleanup | ср | S | 2 |
| `DATA-STREAM-WRITE` | Стриминг не должен переписывать весь JSONB каждые 500 символов | ср | M | 4 |
| `DATA-MIGRATE-LOCK` | Миграции индексов, не блокирующие запись на деплое | низ | S | 2 |

---

## 5. Задачи

### Тема 1 — Закрыть открытые поверхности

Два крита живы в дефолтном деплое: неаутентифицированный эндпоинт, публикующий секретные токены, и целое семейство маршрутов без скоупинга по владельцу. Оба дёшевы относительно радиуса поражения, и команда сейчас считает IDOR закрытым.

#### `SEC-METRICS` — метка метрик это шаблон маршрута, и `/metrics` наружу не отдавать
**критично · S · волна 1**

*Что:* в `correlation_middleware` (`app.py:145-150`) заменить `str(request.url.path)` на шаблон совпавшего маршрута из `request.scope["route"].path` с фолбэком `__unmatched__`; повесить `admin_guard` на `@app.get("/metrics")` (`app.py:284`) и удалить блок `location /metrics` из `nginx.conf`, чтобы Prometheus скрейпил `api:8000` только по внутренней сети. Заодно разделить `/health` на дешёвый статус и админский `/health/detail`.

*Почему:* вся модель безопасности публичных ссылок — неугадываемость токена (`share_mixin.py:51-54` прямо это и утверждает). Каждый заход на `/v1/public/research/{token}` создаёт серию `mas_api_requests_total{path="/v1/public/research/<секрет>"}`, отдаваемую без авторизации на публичном порту SPA. Один `curl` собирает все токены шаринга, которые когда-либо использовались. Плюс неограниченная кардинальность — постоянная утечка памяти в каждом воркере uvicorn.

*Файлы:* `src/api/app.py` · `src/observability/metrics.py` · `web/nginx.conf` · `src/services/share_mixin.py`

*Готово, когда:* тест утверждает, что после `GET /v1/public/research/<token>` вывод `render_metrics()` содержит `/v1/public/research/{token}` и **не** содержит сам токен; `curl http://localhost:8501/metrics` отдаёт 404, Prometheus по-прежнему скрейпит; число серий не растёт на 50 разных исследованиях.

#### `SEC-IDOR` — скоупинг по владельцу для семейства `/v1/tasks` и `/v1/search-jobs`
**критично · M · волна 2**

*Что:* добавить `user_id` в `get_all_tasks`/`get_task`/`update_task`/`get_search_task_job` в обоих сторах (джойн `SearchTaskORM.research_id` на владельца), протащить через `list_tasks`/`get_task`/`update_task` (`research_service.py:192-199`) и отдавать 404 при несовпадении, как уже делает `_ensure_research_access`. Немедленный стоп-гэп в том же PR: три листинговых маршрута под `admin_guard` — это операторские инструменты, персонального смысла у них нет. Структурная альтернатива, если появится настоящая многопользовательность: денормализовать `search_tasks.user_id` из родительского исследования и фильтровать в самом сторе — тогда новый эндпоинт не сможет забыть про guard.

*Почему:* проверено вручную: все девять маршрутов несут только `dependencies=auth_required`, а `list_tasks(self, limit, offset)` вообще не принимает id пользователя. `GET /v1/tasks?limit=500` отдаёт чужие под-вопросы и полные тела скачанных источников; `PATCH /v1/tasks/{id}` позволяет любому залогиненному пометить чужую задачу как FAILED на ходу. `execution-plan.md:45` объявляет это закрытым — что верно для `/v1/research/{id}/*` и неверно для параллельного семейства.

*Файлы:* `src/api/app.py:350-393,433-447` · `src/services/research_service.py:192-199` · `src/repositories/*_task_store.py`

*Готово, когда:* пользователь B получает 404 на каждую задачу, джобу и финализирующую джобу пользователя A.

#### `SEC-ACCOUNT` — текущий пароль при смене, отзываемые сессии, запрет тихого слияния OAuth
**высокая · M · волна 2**

*Что:* добавить `current_password` в `SetPasswordRequest` (`domain/models.py:533`) и проверять его в `AuthMixin.set_user_password` (`auth_mixin.py:62`), если у аккаунта есть настоящий пароль — OAuth-плейсхолдер помечать явно, а не хранить случайный PBKDF2-хеш. Добавить `token_version` (или `password_changed_at`), штамповать в `create_token` (`security.py:61`) и отвергать устаревшие токены в `get_current_user`. В `get_or_create_oauth_user` (`auth_mixin.py:47-53`) не присваивать существующий локальный аккаунт без подтверждения.

*Почему:* одна утёкшая JWT на 7 дней превращается в постоянное владение: держатель POST-ит `/v1/auth/set-password` без проверки текущего, а настоящий владелец не может его выселить — logout лишь удаляет куку. CSRF-мидлварь тут не помогает: `app.py:96-97` явно освобождает Bearer-запросы. Отдельно: подтверждения email нет нигде, поэтому регистрация на чужой адрес тихо захватывает аккаунт, к которому жертва позже придёт через Google.

*Файлы:* `src/domain/models.py:533` · `src/services/auth_mixin.py:47-62` · `src/auth/security.py:61` · `alembic/versions/`

*Готово, когда:* смена пароля без `current_password` → 400, с неверным → 401; токен, выпущенный до смены, → 401 на любом авторизованном роуте; миграция на месте, `test_migration_contract` зелёный.

#### `SEC-LOGIN-LIMITER` — пробросить реальный IP клиента
**высокая · S · волна 1**

*Что:* добавить `proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;` и `X-Real-IP` в блок `/v1/` в `nginx.conf` и запускать uvicorn с явным `--forwarded-allow-ips <адрес nginx>` в `docker-compose.yml:51`.

*Почему:* `request.client.host` для каждого браузерного запроса — это адрес контейнера nginx, поэтому весь трафик SPA делит одно ведро: `auth_rate_limit_per_minute: 10` превращается в 10 попыток в минуту на всю инсталляцию. Один атакующий — или один цикл ретраев — выжигает лимит, и логин ложится для всех.

*Файлы:* `web/nginx.conf` · `docker-compose.yml:51` · `src/auth/login_rate_limit.py`

#### `SEC-ERROR-LEAK` — убрать три обёртки `except Exception → HTTPException(500, str(e))`
**средняя · S · волна 1**

*Что:* удалить широкие обёртки в `optimize_prompt` (`app.py:333-336`), `decompose_prompt` (`:345-348`) и `start_research` (`:428-431`), отдав работу уже зарегистрированному `_service_error_handler` (`app.py:118-122`); логировать через `logger.exception`.

*Почему:* это одновременно утечка информации и баг статус-кодов. `ServiceError` — обычный `Exception`, поэтому обёртки глотают собственные типизированные ошибки сервисного слоя: `ConflictError` из `start_research` («исследование уже идёт», должно быть 409) уезжает к пользователю как 500 с сырым текстом исключения.

*Файлы:* `src/api/app.py:333-336,345-348,428-431` · `src/domain/errors.py`

---

### Тема 2 — Сделать CI настоящим гейтом

Всё уже написано: 516 тестов, контракт дрейфа ORM/миграций, offline eval-гейт, `vue-tsc` и vitest в `package.json`. CI не запускает ничего из этого. Самая дешёвая тема в плане и предпосылка для всех остальных.

#### `CI-PYTEST` — гонять бэкенд-сьют на каждом PR
**высокая · S · волна 1**

*Что:* добавить job с `pytest -q -m "not postgres"` без сервисов (обратная связь меньше минуты) и второй job с полным `pytest -q` против уже описанного digest-пиннутого Postgres — сервис и переменные окружения в workflow уже настроены, это несколько строк. Заодно усилить сам контракт дрейфа: `opts={"compare_type": True, "compare_server_default": True}`.

*Почему:* единственный workflow делает `pip install`, `alembic upgrade head` и один смоук-скрипт — строки `pytest` в нём нет вообще. PR, ломающий CSRF, SSRF-валидацию вебхуков, админскую авторизацию или машину состояний финализации, мержится зелёным. Отдельно обидно с контрактом дрейфа ORM и миграций: он написан, но помечен `-m postgres`, поэтому не запускается вообще нигде — при том что БД в этом же workflow уже поднята.

*Файлы:* `.github/workflows/postgres-smoke.yml` · `tests/test_migration_contract.py`

#### `CI-WEB` — job для фронта: `vue-tsc` и vitest
**высокая · S · волна 1**

*Что:* job `npm ci && npx vue-tsc --noEmit && npm run test` в `web/`. Добавить `@vue/test-utils` + `jsdom` и первые три теста: разбор SSE-кадров и контракт терминального события в `lib/stream.ts`, маппинг ошибок в `lib/api.ts`, рендер цитат в `MarkdownView`.

*Почему:* 5 690 строк Vue/TS покрыты одним 38-строчным тестом паритета i18n, а `vue-tsc` запускается только внутри `web/Dockerfile` при `npm run build` — то есть ошибка типов роняет сборку образа на деплое, а не PR.

*Файлы:* `.github/workflows/` · `web/package.json` · `web/src/lib/stream.ts` · `web/src/lib/api.ts`

#### `CI-EVAL-SMOKE` — подключить eval-гейт и убрать хардкод порта в смоуке
**средняя · S · волна 1**

*Что:* один шаг `python -m eval --fixtures eval/fixtures --gate` — он offline, без ключей и БД, идёт секунды. И заменить хардкод `…@localhost:5433/…` в `scripts/smoke_postgres_runtime.py:151-154` на `settings.resolved_database_url`.

*Почему:* единственный механизм, построенный чтобы ловить регрессии качества исследования, не подключён ни к чему — при том что документация утверждает обратное. Каждое «eval-гейт чист» в `execution-plan.md` означает ручной прогон, о котором внешний контрибьютор не знает.

*Файлы:* `.github/workflows/postgres-smoke.yml` · `scripts/smoke_postgres_runtime.py:151-154` · `eval/`

#### `CI-TOOLING` — pyproject.toml, ruff, coverage, снять wildcard-импорты
**средняя · M · волна 4**

*Что:* завести `pyproject.toml` с `[project]` (тогда `pip install -e .` заменит хаки `sys.path.insert` в `scripts/`), `[tool.ruff]` и `[tool.pytest.ini_options]` вместо `pytest.ini`, плюс `--strict-markers`.

*Почему:* 18 991 строка Python без линтера, форматтера, тайпчекера и покрытия — ровно поэтому дыра в покрытии SQLAlchemy-стора и нулевые тесты экспорта PDF/DOCX прожили незамеченными. Wildcard-импорты сейчас протаскивают `BaseModel`, `datetime`, `urlparse` в чужие пространства имён.

*Файлы:* `pyproject.toml` (новый) · `pytest.ini` · `scripts/run_finalize_worker.py:9-11` · `src/`

#### `TEST-STORE-CONFORMANCE` — один conformance-сьют против обеих реализаций TaskStore
**высокая · L · волна 3**

*Что:* `@pytest.fixture(params=["memory","postgres"])` в `conftest.py` и общий поведенческий сьют поверх неё, чтобы каждый из 61 метода протокола проверялся один раз на бэкенд; postgres-нога скипается только при отсутствии БД.

*Почему:* продакшен-стор (992 строки, 66 методов, дефолт конфига) упомянут в 10 самоскипающихся тестах, тогда как in-memory близнец собирает 208 ссылок. 31 продакшен-метод никогда не исполняется против настоящей БД — включая `try_begin_finalization`. Это же предпосылка для любого структурного рефакторинга.

*Файлы:* `tests/conftest.py` · `tests/postgres_helpers.py` · `src/repositories/protocols.py`

---

### Тема 3 — Работа, которая зависает, дублируется или теряется

Пять способов, которыми исследование застревает или платится дважды. Три из них — правки на несколько строк; общий корень один: механизм починили в одной ветке кода и не перенесли в соседнюю.

#### `JOB-RETRY` — возвращать повторяемые задачи в Redis
**высокая · S · волна 1**

*Что:* в `process_search_task_job` добавить `if self.broker: self.broker.push_search_job(job_id)` в обе ветки ретрая (`research_service.py` ~1968 и ~1993, сразу после сброса задачи в PENDING) и то же самое для финализации.

*Почему:* проверено вручную: обе ветки сбрасывают задачу в PENDING и пишут в лог «scheduled for retry», но ничего не пушат обратно; при этом соседний путь восстановления зависших задач (`job_queue_mixin.py:121-128`) делает ровно этот push с комментарием, объясняющим, зачем он нужен. В `docker-compose` стоит `USE_REDIS_BROKER: "true"` — то есть в поставляемой конфигурации любой транзиентный сбой поиска вешает исследование навсегда.

*Файлы:* `src/services/research_service.py:1960-2002` · `src/services/job_queue_mixin.py:121-128` · `docker-compose.yml:18,45`

#### `JOB-LEASE` — продлевать аренду работающей финализации и отсекать вытесненный раннер
**критично · M · волна 2**

*Что:* двигать `research_finalize_jobs.updated_at` из `FinalizeGraphRunner._checkpoint` (`research_graph.py:91-113`) и `_emit_finalize_progress` (`research_service.py:1465`) дешёвым `UPDATE … WHERE id=:id AND status='running'` и считать «зависшесть» от него. Добавить `lease_epoch`, повышаемый при восстановлении, и сверять его на записи результата.

*Почему:* пока джоба работает, строку никто не трогает, поэтому `recover_stale_research_finalize_jobs` (`sqlalchemy_task_store.py:583-586`) матчит чисто по возрасту. При `finalize_budget_max_seconds: 480`, ограничивающем только граф, но не весь trust-сьют (red-team + ещё 4 LLM-прохода), живая финализация регулярно переживает таймаут — её пере-диспатчат, и две финализации пишут в одно исследование.

*Файлы:* `src/graph/research_graph.py:91-113` · `src/services/research_service.py:1465` · `src/repositories/sqlalchemy_task_store.py:583-586`

#### `REPORT-CONFLICTS-HEADING` — перестать выкидывать раздел «Конфликты» из готового отчёта
**высокая · S · волна 1**

*Что:* в `_inject_conflicts_section` (`analyzer.py:1242`) заменить строгий инлайновый `re.search(r"(?im)^##\s+(Conclusion|Заключение)\s*$")` на уже объявленный в классе `CONCLUSION_HEADING_PATTERN` (`analyzer.py:42-44`), который знает `Итог…`, `Вывод…`, `Bottom Line`.

*Почему:* строгий якорь `\s*$` не совпадает с заголовком, который просит сам системный промпт («End with a short Conclusion / bottom line», `analyzer.py:178/218`), поэтому фолбэк дописывает конфликты в самый конец — после раздела источников, который постобработка потом перестраивает и срезает. Разногласия источников — заявленный дифференциатор продукта — исчезают из отчёта.

*Файлы:* `src/agents/analyzer.py:42-44,178,218,1242`

#### `JOB-CANCEL` — уважать отмену внутри графа финализации
**высокая · S · волна 1**

*Что:* проверка статуса в начале `_run_timed_step` (`research_graph.py:127`) — она уже получает `research_id` — с `FinalizeCancelled`, ловимым в `complete_research_finalization` (`research_service.py:1476`). Одна точка вставки покрывает collect_context / replan / analyze / verify / tie_break.

*Почему:* `grep -n cancel src/graph/research_graph.py` не находит ничего: отмена аккуратно проверяется до и после графа и ни разу внутри. Весь 480-секундный бюджет плюс trust-сьют оплачиваются уже после того, как пользователь нажал «Отмена», и затем выбрасываются.

*Файлы:* `src/graph/research_graph.py:127` · `src/services/research_service.py:1476`

#### `JOB-REPLAN-DEADLOCK` — финализирующие воркеры не должны ждать задачи, которые могут забрать только они сами
**высокая · M · волна 2**

*Что:* выполнять волну replan инлайн в финализирующем воркере — вызывать существующую ветку без брокера (`self.run_search_task(task.id, depth)`, `research_service.py:1200-1203`) вместо круга через Redis с поллингом на `:1188-1199`. Альтернатива крупнее — разделить пулы search- и finalize-воркеров.

*Почему:* `execute_replan_search_pass` делает RPUSH задач и засыпает в `time.sleep`, но единственные потребители — те же три реплики воркера, каждая из которых за проход в один поток забирает ровно одну задачу (`job_worker.py:20-21` сначала search, потом finalize). Достаточно нескольких параллельных HARD, чтобы все три реплики стояли в ожидании работы, которую некому взять.

*Файлы:* `src/services/research_service.py:1188-1203` · `src/workers/job_worker.py:20-21`

#### `STATE-LOCKED-MERGE` — провести все записи `graph_state` через merge под блокировкой строки
**средняя · M · волна 4**

*Что:* заменить незащищённые read-modify-write на `merge_research_graph_state(research_id, patch)` — каждый из них уже имеет форму «прочитать, поставить пару ключей, записать»: `checkpoint_graph_state` (`:1249-1252`), `rename_research` (`:541`), `submit_clarifications` и остальные. Затем удалить `update_research_graph_state` из протокола `TaskStore` (`protocols.py:64-68`), чтобы небезопасный примитив нельзя было вернуть, и добавить явный метод удаления ключа для отзыва публичной ссылки (`share_mixin.py:38`).

*Почему:* безопасный API существует и задокументирован («атомарно мержит patch под блокировкой строки, чтобы конкурентные писатели не затирали ключи друг друга, AUD-014») — но самые горячие писатели на него так и не переведены. В `graph_state` лежат конфликты, аудит цитат, уверенность, план и сообщения чата; потерянное обновление здесь стирает результат уже оплаченной работы.

*Файлы:* `src/services/research_service.py:541,1249-1252` · `src/repositories/sqlalchemy_task_store.py` · `src/services/share_mixin.py:38`

#### `GRAPH-TOPOLOGY` — свести три кодировки графа финализации к одной
**средняя · M · волна 4**

*Что:* сделать langgraph честной обязательной зависимостью (`requirements.lock:45` уже пинит `langgraph==1.2.4`) и удалить `_run_pipeline` (`:449-467`) и `_resume_fallback` (`:498-529`), выразив возобновление как выбор точки входа LangGraph по `state["resume_from_step"]`.

*Почему:* `run()` выбирает фолбэк всякий раз, когда задан `resume_from_step`, поэтому каждый возобновлённый после сбоя прогон исполняет другой граф, чем свежий: на дефолтном конфиге LangGraph маршрутизирует tie_break → collect_context и может запустить replan, а фолбэк — нет. Воспроизводимость исследования зависит от того, падал ли воркер.

*Файлы:* `src/graph/research_graph.py:449-467,498-529` · `requirements.lock:45`

---

### Тема 4 — Один канонический пул источников

Это тема про то, ради чего продукт существует. `[Sn]` сегодня выводится независимо в пяти местах из четырёх разных пулов, а «конфликт» определяется тестом, который не проверяет, что два утверждения вообще про одно и то же.

#### `CITE-POOL` — сохранять один канонический список источников и читать его всем потребителям
**высокая · M · волна 3**

*Что:* заставить `AnalyzerAgent.run_analysis` отдавать наружу тот самый `aggregated_data`, который он использовал, а `FinalizeGraphRunner.run` — возвращать `(report, aggregated_data, effective_prompt, final_tasks)` (все четыре уже лежат в `state`), и записывать таблицу `source_id → url/title/domain` в состояние исследования. Все вкладки читают её, а не пересчитывают.

*Почему:* два дефекта, усиливающих друг друга. `tasks` захватывается на `research_service.py:1487` *до* запуска графа и больше не перечитывается, тогда как replan и tie-break добавляют задачи и переписывают промпт в `effective_prompt` — и оба входа кормят `_detect_topics`, который определяет отбор. В итоге у панели конфликтов, вкладки проверки и чата свой пул и своя нумерация.

*Файлы:* `src/agents/analyzer.py` · `src/graph/research_graph.py` · `src/services/research_service.py:1487` · `src/services/trust_report_mixin.py`

#### `CITE-CHAT` — чат должен цитировать нумерацию отчёта и видеть отчёт целиком
**высокая · M · волна 3**

*Что:* в `generate_research_answer` (`research_service.py:955-966`) сохранять канонический `source_id` каждого найденного источника вместо перенумерации после `_rank_sources_for_question`; расширить `ChatMessage` полем `sources` и передавать явную карту в `MarkdownView`.

*Почему:* два видимых отказа в функции, которая продаётся как «grounded follow-up». `[S3]` в ответе чата — другой источник, чем `[S3]` в отчёте, поэтому пользователь, который решит перепроверить, попадёт не на ту страницу — прямая противоположность проверяемости.

*Файлы:* `src/services/research_service.py:955-966` · `src/agents/chat.py` · `web/src/components/MarkdownView.vue`

#### `CONFLICT-ADJUDICATE` — подтверждать кандидатов в конфликты LLM перед показом
**высокая · M · волна 3**

*Что:* оставить существующую эвристику (`rust_accel.py:462-481`) дешёвой стадией recall, а топ-5 пар прогонять одним вызовом дешёвой модели: «противоречат ли эти два утверждения друг другу про один и тот же предмет и период?» — да/нет плюс строка обоснования. В отчёт попадают только подтверждённые.

*Почему:* весь текущий тест — два предложения делят ≥2 информативных токена, и либо у них разные флаги отрицания, либо разные числа при ≥3 общих токенах. Нет проверки, что речь про одну сущность, метрику или период, а `has_negation` ищет подстроку. «Выручка выросла на 12% в 2024» и «выручка выросла на 12% в 2025» — конфликт.

*Файлы:* `src/core/rust_accel.py:462-481` · `src/agents/analyzer.py`

#### `CONFLICT-LEXICONS` — сделать словари конфликтов и доказательств языковыми
**высокая · M · волна 3**

*Что:* вынести `STOPWORDS`, `CONFLICT_GENERIC_TOKENS` и `NEGATION_TOKENS` (`analyzer.py:47-71`) в таблицу по языкам в `language_utils.py`: русское отрицание (`не`, `нет`, `без`, `отсутствует`), испанское (`no`, `sin`, `nunca`) и русские/испанские стоп-слова.

*Почему:* все три словаря — английские слова, скармливаемые и движку конфликтов, и evidence-mapper'у. На русских источниках — а это основная локаль — `has_negation` не срабатывает никогда, то есть триггер отрицания мёртв; при этом `который`, `также`, `компании` проходят фильтр стоп-слов и считаются значимым пересечением.

*Файлы:* `src/agents/analyzer.py:47-71` · `src/agents/language_utils.py` · `src/agents/evidence_mapper.py`

---

### Тема 5 — Стоимость, ёмкость и злоупотребление изнутри

Дальше страницы логина нет ни одного throttle, квоты или учёта трат — grep по `src/` на quota/budget/spend возвращает одни комментарии. Плюс две правки на несколько строк, каждая из которых прямо сейчас жжёт деньги и время на каждом прогоне.

#### `COST-GAP-CALLS` — проверять условие ветвления до вызова gap-анализа, а не после
**высокая · S · волна 1**

*Что:* в `_collect_context` (`research_graph.py:178-190`) сначала вычислять `branch_possible` (`_is_deep_loop` ∧ лимит replan ∧ бюджет ∧ не `branch_stalled`) и только потом вызывать `suggest_follow_up`.

*Почему:* проверено вручную: `suggest_follow_up` вызывается безусловно на строке 178, а гейт стоит ниже, на 186-189. Каждая финализация EASY и MEDIUM — а MEDIUM это дефолт в SPA — оплачивает от 1 до 4 последовательных LLM-вызовов, результат которых выбрасывается.

*Файлы:* `src/graph/research_graph.py:178-190` · `src/agents/replan.py`

#### `COST-EARLY-STOP` — сделать ранний останов экстракции достижимым
**высокая · S · волна 1**

*Что:* условие невыполнимо по построению: `_early_stop_success_target` возвращает `min(candidate_count, max(max_sources*2, concurrency))`, а `max_candidate_urls` ровно `2 × source_limit` во всех трёх профилях (8/16, 16/32, 24/48). Порог всегда равен полному числу кандидатов. Опустить цель до самого лимита источников с запасом на дедуп.

*Почему:* вся машинерия `submit_candidates` / early-stop (`search.py:385-480`) в продакшене — мёртвый код. HARD это 6 задач × 48 URL = 288 загрузок страниц на исследование, при конкурентности 4 и таймауте 12 с — до ~144 с чистого скачивания на задачу, даже когда нужные источники набраны на первой трети.

*Файлы:* `src/agents/search.py:385-480` · `src/search_depth_profiles.py`

#### `PERF-WORKER-LOOP` — строить сервис один раз и прекратить полное сканирование `researches`
**высокая · S · волна 1**

*Что:* вынести `create_research_service()` из цикла в `scripts/run_finalize_worker.py:25-33` — собрать один раз в `main()` и передать в `JobWorker`; мемоизировать engine в `src/db/session.py`; дать `run_queue_maintenance` собственный интервал 30-60 с вместо каждого цикла.

*Почему:* каждый цикл, в каждой реплике, создаётся и бросается новый SQLAlchemy engine (QueuePool 10+20) и Redis-клиент, а каждая строка `researches` — вместе с полным текстом отчёта и JSON графа — загружается в память воркера ради перекомпактизации. Три реплики делают это параллельно.

*Файлы:* `scripts/run_finalize_worker.py:25-33` · `src/db/session.py` · `src/workers/maintenance_worker.py`

#### `PERF-SSE` — перевести SSE на async
**высокая · M · волна 4**

*Что:* сделать `event_stream` (`app.py:688`) и мост чата (`:630`) асинхронными генераторами: `redis.asyncio` для pubsub и `await asyncio.sleep()` вместо блокирующих `listener.get_message(timeout=15)` / `time.sleep(1.0)`, оставшееся блокирующее — в `run_in_threadpool`.

*Почему:* все роуты — синхронные `def`, поэтому синхронный SSE-генератор держит один из 40 дефолтных токенов AnyIO практически на всё время стрима. При ~40 одновременных зрителях на воркер каждый второй запрос — включая `/health` — встаёт в очередь за ними.

*Файлы:* `src/api/app.py:630,688` · `src/brokers/redis_broker.py`

#### `ADMIT-ATOMIC` — атомарный приём и его проверка на каждом входе в тратящее состояние
**высокая · M · волна 2**

*Что:* вынести проверку из `start_research` (`:302-311`) в `_admit_or_raise(user_id)` и вызывать её также из `submit_clarifications` (`:716`) и `approve_research_plan` (`:796`); сделать проверку-и-вставку атомарной методом `add_research_if_under_limit(...)` в сторе.

*Почему:* `start_research` — единственная точка контроля обоих лимитов, а `CLARIFYING` и `PLAN_REVIEW` отсутствуют в `_IN_FLIGHT_STATUSES`. Значит N запросов `POST /v1/research {plan_first:true}` паркуются неучтёнными, а затем N вызовов `/plan/approve` переводят их всех в работу разом, обходя и персональный, и глобальный лимит.

*Файлы:* `src/services/research_service.py:302-311,716,796` · `src/repositories/protocols.py`

#### `ABUSE-ROUTES` — персональные лимиты, учёт трат и `/v1/decompose` без сиротской работы
**высокая · M · волна 2**

*Что:* три части. (1) `decompose_prompt` (`:201-218`) перестаёт enqueue-ить поисковые джобы — это превью декомпозиции. (2) Персональный throttle на пять LLM-роутов. (3) Счётчик `mas_llm_cost_usd_total` по пользователю и исследованию.

*Почему:* дальше логина нет ни throttle, ни квоты, ни учёта: `enforce_auth_rate_limit` висит ровно на двух маршрутах. `/v1/decompose` — самая острая дыра: он делает LLM-вызов и создаёт поисковые джобы, которые никакое исследование потом не заберёт, — и повторять его можно в цикле.

*Файлы:* `src/services/research_service.py:201-218` · `src/api/app.py` · `src/observability/metrics.py`

#### `PERF-DB` — индексы на JSONB-выборки и не тянуть блобы отчётов в списки
**средняя · S · волна 2**

*Что:* миграция с `ix_researches_thread_id ON researches ((graph_state->>'thread_id'))`, тем же для `share_token`, и составным `ix_researches_user_created (user_id, created_at DESC)`; перевести `list_researches` и `list_thread_researches` на явный список колонок вместо всей строки.

*Почему:* `GET /v1/threads/{id}` — последовательное сканирование с де-TOAST-ом `graph_state` по каждой строке таблицы и неограниченной выдачей. Горячий путь приёма ещё хуже: `_count_active` выполняется на каждом `start_research`.

*Файлы:* `alembic/versions/` · `src/repositories/sqlalchemy_task_store.py:160-179`

#### `COST-DDG` — DuckDuckGo не должен ждать два проигравших запроса
**средняя · S · волна 1**

*Что:* в `providers/search.py:81-94` заменить `with ThreadPoolExecutor(...)` на явный executor и `shutdown(wait=False, cancel_futures=True)` перед возвратом первого успеха. Лучше — сначала пробовать бэкенд `api` в одиночку и разворачивать веер только при его отказе.

*Почему:* выход из-под `with` вызывает `shutdown(wait=True)`, поэтому запрос, на который `api` ответил за 2 с, возвращается только после того, как `html` и `lite` упрутся в свои ~20 с таймаута. Пол задержки на запрос — самый медленный бэкенд, а не самый быстрый; плюс тройная нагрузка на источник, который и так лимитирует.

*Файлы:* `src/providers/search.py:81-94`

---

### Тема 6 — Видимость и безопасная эксплуатация

Streamlit-панель операций была снята под обещание, что её заменит Grafana (`frontend-roadmap.md:28`). Замена не построена: в провижининге есть датасорсы и нет ни одного дашборда и ни одного правила алертинга.

#### `OPS-OBSERVABILITY` — дашборды, правила алертов и скрейп воркеров
**высокая · M · волна 4**

*Что:* добавить `ops/grafana/provisioning/dashboards/` с закоммиченным JSON: триада RED из `mas_api_request_duration_seconds` (после `SEC-METRICS` метка станет пригодной), `mas_queue_jobs` по типу и статусу, `mas_worker_jobs_total` и панель Loki по `research_id`. Плюс правила алертов и скрейп-таргеты для трёх воркеров.

*Почему:* сейчас ничто не сигналит, когда очередь финализации растёт, когда учащаются падения воркеров, когда копятся dead-letter джобы. Обещанная замена операционному UI существует как два датасорса.

*Файлы:* `ops/grafana/provisioning/` · `ops/prometheus/prometheus.yml`

#### `OPS-BOOT-HEALTH` — деплой без рабочего LLM не должен рапортовать healthy
**средняя · S · волна 2**

*Что:* в `bootstrap.py:90-110` сузить `except Exception`, чтобы ошибка конфигурации (нет `DEEPSEEK_API_KEY`) поднималась наверх; если деградированный старт нужен намеренно — выставлять флаг и отдавать `"status": "degraded"` из `get_health_status`.

*Почему:* `DeepSeekProvider` честно падает без ключа, но bootstrap глотает это в warning — и optimizer, orchestrator, analyzer, chat, clarifier, red_team, comparison, stance, cross_language остаются `None`, а старт завершается успешно. Оркестратор рапортует зелёный статус, пользователь создаёт исследование, и оно умирает на первом же агенте.

*Файлы:* `src/bootstrap.py:90-110` · `src/services/research_service.py:1795`

#### `OPS-BACKUP` — путь бэкапа и восстановления для тома, где лежит всё
**средняя · S · волна 2**

*Что:* `scripts/backup_postgres.sh` и `scripts/restore_postgres.sh` вокруг `pg_dump -Fc` / `pg_restore` против сервиса `db` (нужные бинарники уже есть в digest-пиннутом образе), плюс раздел README про расписание, ретенцию и учение по восстановлению.

*Почему:* `grep -rn -i "pg_dump|pg_restore|backup"` по репозиторию не находит ничего. Каждое исследование, отчёт, публичная ссылка, аккаунт и хеш пароля живут в одном неуправляемом именованном томе, а операционный раздел README заканчивается на командах остановки.

*Файлы:* `scripts/` · `README.md` · `docker-compose.yml`

#### `OPS-RETENTION` — окно хранения для исследований и скачанных тел страниц
**средняя · M · волна 4**

*Что:* добавить `research_retention_seconds: int = 0` (0 = хранить вечно, текущие деплои не меняются) рядом с существующим блоком ретенции в `config.py:92-93` и `cleanup_old_researches(older_than)` в протокол и оба стора, одним каскадным DELETE. Заодно дать `compact_research_graph_trails` ограниченный рабочий набор (фильтр по `updated_at`, выборка только `id, graph_trail`, батчи через `.yield_per()`).

*Почему:* цикл обслуживания подчищает только `search_task_jobs`, `research_finalize_jobs` и `search_cache`. `researches`, `search_tasks` и `search_results` (до 10 000 тел страниц на исследование) не подчищает ничто — том растёт монотонно.

*Файлы:* `src/config.py:92-93` · `src/repositories/protocols.py` · `src/workers/maintenance_worker.py`

---

### Тема 7 — Корректность фронтенда

Один баг ломает grounded-чат в дефолтной конфигурации целиком; остальные два — про то, что пользователь видит, когда что-то пошло не так.

#### `FE-CHAT-CSRF` — слать заголовки авторизации и CSRF из клиента стрима чата
**высокая · S · волна 1**

*Что:* вынести из `lib/api.ts:55-64` хелпер `authHeaders(method)` (Bearer, если есть токен, плюс `X-CSRF-Token` из куки для небезопасных методов) и подмешать его в `fetch` внутри `streamChatAnswer` (`lib/stream.ts:66-71`).

*Почему:* проверено вручную: `streamChatAnswer` обходит `request()` целиком и шлёт только `Content-Type`. Собственная CSRF-мидлварь приложения (`app.py:89-100`) требует заголовок при куки-аутентификации — то есть каждый запрос стрима чата в дефолтном режиме (`auth_disabled: false`) получает 403.

*Файлы:* `web/src/lib/stream.ts:66-71` · `web/src/lib/api.ts:55-64` · `src/api/app.py:89-100`

#### `FE-FAILURES` — показывать ошибки экспорта и гарантировать терминальное событие стрима
**средняя · S · волна 4**

*Что:* в `exportReport` (`ArtifactPanel.vue:409-427`) заменить `if (!res.ok) return;` на установку сообщения об ошибке, обернуть fetch в try/catch и показывать ошибку в панели загрузки. Плюс гарантировать терминальное событие, если тело SSE закрылось без него.

*Почему:* сейчас неудавшийся экспорт PDF/DOCX/HTML показывает многоточие и затем ничего — ни файла, ни ошибки, ни способа понять, истекла сессия или упала генерация; для оператора это тоже невидимо, потому что статус-код никуда не попадает.

*Файлы:* `web/src/components/ArtifactPanel.vue:409-427` · `web/src/lib/stream.ts`

#### `FE-ERRORS` — типизированные ошибки API, локализованные сообщения и восстановление на 401
**средняя · M · волна 4**

*Что:* в `request()` (`lib/api.ts:52-76`) разбирать тело как JSON и бросать типизированный `ApiError { status, detail }`; на 401 — сбрасывать токен, чистить стор и уводить на `/login`; частые статусы маппить в ключи i18n.

*Почему:* любая обычная ошибка — неверный пароль, 409 лимита, 429 — показывается пользователю сырым JSON-блобом вида `401 {"detail":"Invalid email or password"}`, что читается как падение приложения. На 401 никто не вызывает `setAuthToken(null)` и не редиректит.

*Файлы:* `web/src/lib/api.ts:52-76` · `web/src/stores/auth.ts` · `web/src/i18n/index.ts`

---

### Тема 8 — Качество загрузки, язык и юридическое

Система сама поощряет первоисточники в скоринге — и не умеет их читать. Плюс шесть детекторов языка, которые расходятся по построению, и отсутствующая лицензия.

#### `ACQ-PDF` — читать PDF, а не кормить мохибейк HTML-экстрактору
**высокая · M · волна 3**

*Что:* возвращать content-type из фетча: `safe_fetch_document(url) -> tuple[bytes, str] | None`, отдающий `response.content` и нормализованный тип, с сохранением ре-валидации на каждом хопе. Ветвиться в `ContentExtractor.extract_content` и извлекать текст PDF отдельной веткой.

*Почему:* `safe_fetch_html` проверяет статус и размер и возвращает `response.text`, нигде не глядя на Content-Type, поэтому PDF декодируется в мохибейк; дальше пиннутая trafilatura 2.1.0 отбраковывает его как `is_dubious_html`, и источник молча теряется. Именно PDF — это препринты, отчёты регуляторов и научные статьи, которым скоринг качества даёт наивысший грейд.

*Файлы:* `src/net_safety.py` · `src/providers/search.py` · `src/source_quality_policy.py`

#### `ACQ-COOLDOWN` — один таймаут не должен навсегда вдвое снижать терпимость к домену
**средняя · M · волна 4**

*Что:* в `ExtractionDomainRegistry.record` (`search.py:316-337`) убрать дизъюнкт `timeout_count >= 1`, чтобы таймауты подчинялись `search_domain_fail_threshold` как все прочие сбои, и затухать счётчикам по истечении кулдауна.

*Почему:* реестр — модульный синглтон без сброса и без тестов. `timeout_count` обнуляется только успехом, поэтому один таймаут навсегда снижает терпимость домена с двух последовательных сбоев до одного — на всё время жизни процесса, для всех пользователей и всех исследований.

*Файлы:* `src/providers/search.py:316-337`

#### `LANG-SINGLE-SOURCE` — определять язык один раз, хранить и удалить пять остальных детекторов
**средняя · M · волна 3**

*Что:* оставить одну реализацию — `cross_language.py:39-84 detect_language` (по доминирующему письму, знает zh/ja/ko/ar/he/hi/el). Определять язык запроса один раз при создании исследования, сохранять на строке и передавать сохранённое значение всем потребителям.

*Почему:* шесть детекторов расходятся по построению. `_detect_report_language` смотрит на *весь готовый отчёт* и возвращает русский по одному кириллическому символу — а отчёт всегда заканчивается автоматически перестроенным разделом источников из заголовков скачанных страниц.

*Файлы:* `src/agents/cross_language.py:39-84` · `src/agents/language_utils.py` · `src/agents/analyzer.py`

#### `LEGAL-LICENSE` — добавить LICENSE
**средняя · S · волна 1**

*Что:* положить LICENSE в корень под выбранную позицию — Apache-2.0 под то разрешительное self-hosted распространение, которое описывает `deep-research-roadmap.md:17`, либо AGPL-3.0, если цель — не дать поднять хостинговый клон, — и сослаться на неё из README.

*Почему:* проверено вручную: ни LICENSE, ни COPYING, ни NOTICE в репозитории нет, и слово «licen» не встречается ни в README, ни в docs. Без явного гранта код по умолчанию «все права защищены» — то есть инструкции по развёртыванию в README никому юридически ничего не разрешают.

*Файлы:* `LICENSE` (новый) · `README.md`

#### `DATA-LIFECYCLE` — удаление аккаунта, FK на владельца, жизненный цикл публичных ссылок
**средняя · M · волна 4**

*Что:* миграция, дающая `researches.user_id` (`models.py:37`, сейчас голый `String(36)`) настоящий FK на `users(id)` с `ON DELETE CASCADE`, чтобы уже существующие каскады на задачи и тела страниц сработали; `delete_user(user_id)` в протокол и оба стора; отзыв всех выданных публичных токенов; CHECK-констрейнты на колонки статусов, сгенерированные из Python-энумов; предупреждение о содержимом, уходящем в LangSmith.

*Почему:* возможности удалить аккаунт нет, FK нет — поэтому ручной `DELETE FROM users` оставляет все данные пользователя на месте: недостижимыми через API, но по-прежнему публично отдаваемыми по каждой выданной им ссылке. Это в точности обратное удалению.

*Файлы:* `src/db/models.py:37` · `alembic/versions/` · `src/repositories/protocols.py` · `src/services/share_mixin.py`

---

### Тема 9 — Схема и миграции

Слой хранения сделан аккуратно (см. §2). Слабые места структурные, а не небрежные.

#### `DATA-INDEX-QUEUE` — индексы под захват задач, подметание зависших и очистку
**средняя · S · волна 2**

*Что:* добавить (все через `CREATE INDEX CONCURRENTLY`) частичные индексы захвата `ix_search_task_jobs_pending ON search_task_jobs (created_at) WHERE status = 'pending'` и такой же для `research_finalize_jobs`, плюс составные `(status, updated_at)` под sweep зависших и `(status, created_at)` под cleanup.

*Почему:* существующая миграция 13 проиндексировала только `status` — колонку крайне низкой селективности, — а по `created_at`/`updated_at` нет ни одного индекса. Поэтому каждый захват задачи делает скан по статусу с сортировкой, а каждый проход обслуживания в каждой из трёх реплик — тем более. Частичный индекс здесь крошечный и превращает захват в один index scan без сортировки.

*Файлы:* `alembic/versions/20260521_000013_add_status_indexes.py` · `src/repositories/sqlalchemy_task_store.py` · `src/db/models.py`

#### `DATA-STREAM-WRITE` — стриминг отчёта не должен переписывать весь JSONB-блоб каждые 500 символов
**средняя · M · волна 4**

*Что:* вынести `partial_report` и `partial_reasoning` из `graph_state` в отдельные TEXT-колонки, чтобы колбэк стриминга трогал только их.

*Почему:* сейчас каждые 500 символов генерации отчёт целиком дописывается в `graph_state` — а это тот же блоб, где лежат конфликты, аудит цитат, уверенность, план и сообщения чата. Каждая такая запись переписывает и де-TOAST-ит весь документ, то есть на длинном HARD-отчёте это сотни полных перезаписей растущего JSON, и они же — самые вероятные участники гонки из `STATE-LOCKED-MERGE`.

*Файлы:* `src/graph/research_graph.py` · `src/repositories/sqlalchemy_task_store.py` · `src/db/models.py`

#### `DATA-MIGRATE-LOCK` — миграции индексов, не блокирующие запись на деплое
**низкая · S · волна 2**

*Что:* в миграциях 13, 17 и 18 обернуть создание индексов в `with op.get_context().autocommit_block():` и выдавать `CREATE INDEX CONCURRENTLY IF NOT EXISTS`, с `DROP INDEX CONCURRENTLY IF EXISTS` в downgrade.

*Почему:* три миграции строят индексы обычным `CREATE INDEX`, который берёт блокировку записи на таблицу, а `docker-compose` гейтит запуск всех сервисов на завершении контейнера `migrate`. На пустой базе это незаметно; на живой с накопленными `search_results` деплой встаёт на время построения. Дёшево сделать правильно сейчас, дорого обнаружить потом.

*Файлы:* `alembic/versions/20260521_000013…`, `20260622_000017…`, `20260622_000018…` · `docker-compose.yml`

---

## 6. Чего делать не стоит

Решения такой же ценности, как и сам список работ.

**Не браться сейчас за полный рефакторинг god-объекта `ResearchService`.** 141 метод в базовом классе и шести миксинах — реальная связанность, но разделение на миксины было осознанным решением (AUD-030), а привычный аргумент «тайпчекер это не проверит» здесь не работает: тайпчекера нет вообще. Взять только два дешёвых среза: снятие wildcard-импортов в составе `CI-TOOLING` и (позже, опционально) вынос 683 строк чистой логики «метрики на входе — алерты на выходе» из `operational_health_mixin` в модульные функции. Заходить дальше до появления conformance-сьюта из волны 3 — способ сломать работающую систему.

**Не собирать пока Rust-модуль в образ и CI.** Это уже ваш собственный открытый пункт (`execution-plan.md:92`, 0.4), и ему нужно решение, а не повторное упоминание. Но сначала о мине: `native/text_processing/src/lib.rs:215/236/915` сравнивают `str::len()` (байты) там, где Python-фолбэк сравнивает символы. Сборка модуля молча уполовинит окно предложения и порог длины токена для кириллицы и утроит для CJK — изменится набор предложений, попадающих в анализ конфликтов и доказательств. Либо сначала починить три сравнения на `.chars().count()` и добавить тест паритета, либо честно пометить модуль как dev-only в README.

**Не «чинить» смоук в CI добавлением настоящего `DEEPSEEK_API_KEY` в секреты.** Путь финализации специально сделан работающим офлайн через `SMOKE_ANALYZER_REPORT` (`config.py:87`, `bootstrap.py:27,82-83`); единственный реальный блокер — хардкод порта 5433 в `smoke_postgres_runtime.py:151-154`. Ключ означал бы живые платные вызовы на каждый push в main.

**Не выносить `thread_id` и `share_token` из `graph_state` в настоящие колонки.** Решение «без миграции» зафиксировано осознанно (`execution-plan.md:51`), а индекс по выражению `(graph_state->>'thread_id')` даёт весь выигрыш по запросам, не отменяя это решение. Промоцию в колонку приберечь для случая, когда JSON-доступ действительно станет проблемой корректности. Одно исключение подтвердилось: `partial_report` и `partial_reasoning` выносить **надо** (`DATA-STREAM-WRITE`) — там проблема не в чтении, а в записи.

**Не начинать эпик доступности.** Он уже описан как неотгруженный пункт F4 (`frontend-roadmap.md:263`) рядом с темами и горячими клавишами. Из него стоит выдернуть вперёд ровно одну строку: следить за `ui.locale` в `App.vue` и выставлять `document.documentElement.lang` — `web/index.html` жёстко задаёт `lang="ru"`, и английские и испанские страницы читаются скринридером с русской фонетикой.

**Не добавлять пока второго LLM-провайдера.** Протокол `LLMProvider` существует, то есть цена абстракции уже уплачена, но отказ единственного провайдера далеко не главный риск здесь; строить FallbackProvider до того, как `ABUSE-ROUTES` даст видимость трат, — значит добавить второй способ тратить деньги, которые вы ещё не умеете измерять. А вот `README.md:46` (`deepseek-chat`) стоит примирить с дефолтом `deepseek-v4-pro` в `config.py:8` — это правка на пять минут.

**Не удалять токен из localStorage «ради безопасности».** Сессионная кука — `samesite="lax"` (`app.py:181`), поэтому любой деплой, где `VITE_API_BASE` указывает на другой origin, потеряет аутентификацию на каждом POST. Bearer-фолбэк несущий.

**Не считать отсутствие CSP в nginx срочным.** Единственный сток `v-html` кормится из `MarkdownIt({ html: false })` с валидацией href и экранированием атрибутов — известного пути инъекции нет, это эшелонирование, а не эксплуатируемая дыра. Стоящая половина — одна строка: `location = /index.html { add_header Cache-Control "no-cache"; }`, иначе закешированный после деплоя `index.html` запрашивает хеш-бандлы, которых больше нет, и отдаёт белый экран.

**Не переписывать в этом плане разбиение отчёта на секции** (настоящий outline-шаг вместо нарезки отсортированного пула по индексам, `analyzer.py:1446-1456`). Это реальный дизайнерский пробел и работа на L, но измерить улучшение нельзя, пока волна 3 не сделала цитаты достоверными, а eval меряет замороженные фикстуры. Ставить после — вместе с работой над live-раннером eval.

**Не собирать оставшуюся мелочь в отдельный батч:** мёртвый кластер `_extract_candidate_claims` (`analyzer.py:1107-1178`), семь почти одинаковых парсеров JSON из LLM, шесть неработающих ключей `analyzer_*` в `config.py:113-118`, мёртвая фикстура `client` в conftest, мёртвая цепочка события `@refreshed`. Каждое — 15 минут, и ни одно не стоит отдельного PR: удалять попутно, когда уже открыт файл, остальное найдёт ruff.

---

## 7. Что нужно решить перед стартом

| Вопрос | Варианты |
|---|---|
| **Объём** | Волна 1 целиком (15 задач, 14 из них — S) — разумный первый шаг: ничего не ломает, каждый пункт откатывается отдельно, после неё CI начинает ловить регрессии сам. |
| **Лицензия** | Apache-2.0 (разрешительная, под self-hosted-позиционирование из роадмапа) или AGPL-3.0 (не даёт поднять хостинговый клон). Единственный пункт волны 1, который нельзя решить за владельца. |
| **Окружение** | На машине разработки нет Python и Rust — есть Docker 29.7 + Compose v5.4 и Node 24. Бэкенд-тесты гонять через Compose или ставить Python 3.11 локально. |
| **Rust-модуль** | Три сравнения байтов вместо символов в `lib.rs` означают, что собранный модуль ведёт себя иначе, чем Python-фолбэк, на кириллице. Чинить паритет и включать в сборку — или пометить dev-only и снять с роадмапа. |

---

## 8. Статус на 25.09.2026 и волна 5

> Повторный аудит против `0f05953` (main после мержа `feature/admin-panel`). Метод: шесть проверяющих сверяли каждую из 44 задач с её «Готово, когда» по текущему коду, четыре аудитора прошли код, влитый после плана (админ-панель, телеметрия, настройки аккаунта, учёт стоимости, консоль агентов), и отдельно искали «близнецов» уже закрытых дефектов. Затем девять утверждений, подтверждённых только одним источником, были адверсарно перепроверены с воспроизведением скретч-тестами. Все подтвердились; два — с уточнением масштаба.

### 8.1 Что произошло

Волны 1–4 закрыли все 44 задачи коммитами. Затем в main влился `feature/admin-panel` (`f76c076`, ~15 тыс. строк) — **с красным Quality Gates**: lint и падающий InMemoryTaskStore чинились уже после мержа (`58d34aa`, `0f05953`). Этот код ни разу не проходил через линзу плана, и тезис §1 — «починили здесь, забыли в близнеце» — повторился почти дословно: новые пути заново открыли ровно те классы дефектов, которые волны закрывали.

Самое серьёзное — **захват админ-аккаунта без пароля** (`2120223`, «seamless initial password setup»): если админ входил только через Google, любой, кто знал его email, логинился с любым паролем от 6 символов, становился админом и выселял владельца. Тест `test_admin_initial_password_and_oauth_login` утверждал это как задуманное поведение.

**Итог повторного аудита по 44 задачам (до волны 5): 21 выполнена · 15 частично · 8 откатились.** После волны 5 все они доведены до своего «Готово, когда», кроме хвостов, перечисленных в §8.4.

### 8.2 Задачи волн 1–4, которые не дошли до «Готово, когда»

Выполнены полностью и не тронуты мержем: `SEC-IDOR`, `SEC-ERROR-LEAK`, `CI-PYTEST`, `CI-EVAL-SMOKE`, `JOB-CANCEL`, `JOB-REPLAN-DEADLOCK`, `REPORT-CONFLICTS-HEADING`, `CITE-POOL`, `ACQ-PDF`, `COST-GAP-CALLS`, `COST-EARLY-STOP`, `PERF-WORKER-LOOP`, `COST-DDG`, `ACQ-COOLDOWN`, `OPS-BOOT-HEALTH`, `DATA-INDEX-QUEUE`, `DATA-STREAM-WRITE`, `DATA-MIGRATE-LOCK`, `FE-CHAT-CSRF`, `FE-FAILURES`, `LEGAL-LICENSE` (выбрана MIT). Хвосты некоторых из них тоже закрыты в волне 5.

| ID | Статус | Что было не так | Закрыто в волне 5 |
|---|---|---|---|
| `SEC-ACCOUNT` | откат | захват админа без пароля; тихое слияние Google-входа с локальным аккаунтом для админ-email вернулось; регистрация на адрес из `ADMIN_EMAILS` сразу давала админку | `W5-ADMIN-TAKEOVER` |
| `SEC-LOGIN-LIMITER` | частично | `--forwarded-allow-ips='*'` + дописывающий `X-Forwarded-For` в nginx: IP выбирает клиент, лимитер обходится сменой заголовка; новые `extract_client_ip` доверяли `CF-Connecting-IP` | `W5-CLIENT-IP`, `W5-AUTH-THROTTLE` |
| `SEC-METRICS` | частично | `mas_llm_cost_usd{user_id,research_id}` — неограниченная кардинальность и идентификаторы на `/metrics`; e2e-теста из критерия не было | `W5-METRICS` |
| `TEST-STORE-CONFORMANCE` | откат | без Postgres пропускалась и memory-нога (0 из 58 тестов в CI-джобе без БД); 20 из 84 методов протокола не покрыты, среди них 17 новых | `W5-TESTS` (частично), `W5-CONFORMANCE` |
| `JOB-RETRY` | частично | ретрай сброшенной в FAILED задачи никогда не выполнялся: финализация ставилась в очередь раньше решения о повторе | `W5-SEARCH-RETRY-ORDER` |
| `JOB-LEASE` | откат | событие `completed` проверяло аренду уже завершённой джобы → `FinalizeLeaseLost` на каждой успешной финализации: webhook в продакшене не срабатывал ни разу | `W5-LEASE-COMPLETED` |
| `STATE-LOCKED-MERGE` | откат | `reset_research_for_retry` и `append_research_graph_event` — снова read-modify-write без блокировки; `append_research_message` и восстановление зависших джоб мержили значения, прочитанные до блокировки | `W5-LOCKED-MERGE`, `W5-TRAIL` |
| `GRAPH-TOPOLOGY` | частично | возобновление с checkpoint `verify` падало `KeyError('__end__')` до dead-letter; `branch_stalled` и дедлайн не сохранялись | `W5-RESUME-END` |
| `PERF-SSE` | откат | синхронная запись в БД на event loop в каждом авторизованном запросе (middleware), в админском SSE и в чат-стриме | `W5-TELEMETRY`, `W5-ADMIN-STREAM` |
| `ADMIT-ATOMIC` | откат | `/retry` переводил в тратящее состояние мимо admission и LLM-лимита; сам роут всегда отдавал 500 (`ResearchStatus.TIMEOUT` не существует) | `W5-RETRY` |
| `ABUSE-ROUTES` | частично | `GET /summary` — до 4 LLM-вызовов на каждый GET; `/v1/telemetry/event` без авторизации и лимитов | `W5-SUMMARY-LLM`, `W5-TELEMETRY` |
| `PERF-DB` | откат | админские запросы грузили все строки в Python, тянули `graph_state` целиком, N+1 на пользователя, сортировка только внутри страницы | `W5-ADMIN-QUERIES`, `W5-EXPORTS` |
| `OPS-RETENTION` | откат | новые таблицы `user_events`, `user_sessions`, `admin_audit_logs` без ретенции; heartbeat каждые 30 с — вечная строка | `W5-TELEMETRY` |
| `OPS-OBSERVABILITY` | частично | панели стоимости запрашивали несуществующую метрику; алерт dead-letter на `increase()` от gauge; нет алертов на отказы воркеров и рост очереди финализации | `W5-METRICS` |
| `OPS-BACKUP` | частично | в ранбуке неверный порт health-check; учение по восстановлению нигде не зафиксировано | `W5-DOCS` (порт); учение — за владельцем |
| `DATA-LIFECYCLE` | частично | удаление аккаунта из Settings не работало никогда (клиент не слал `confirm`); копии промптов в `user_events` переживали удаление исследования | `W5-FE-ACCOUNT`, `W5-TELEMETRY` |
| `FE-ERRORS` | частично | новые вьюхи снова показывали сырые `401 {...}`; Settings целиком на русском; неверный текущий пароль выкидывал на логин | `W5-FE-ERRORS-I18N`, `W5-FE-ACCOUNT` |
| `CI-WEB` | частично | не было `@vue/test-utils`/jsdom и теста цитат в `MarkdownView` | `W5-TESTS` |
| `CI-TOOLING` | частично | нет порога покрытия, `pytest-cov` не пиннут, остались `import *`-шимы; тайпчекера нет (см. §8.5) | `W5-CONFORMANCE` |
| `CITE-CHAT` | частично | явная карта источников перетиралась URL из текста; ответы чата ссылались на `[Sn]` вне топ-12 без ссылки | `W5-FE-MISC`, `W5-LANG` |
| `LANG-SINGLE-SOURCE` | частично | экспорт PDF/DOCX и аудит цитат со своими детекторами; новые шаги трассировки захардкожены по-русски и попадают в английские отчёты | `W5-LANG` |
| `CONFLICT-LEXICONS` | частично | `reason` конфликтов на английском попадает в русский отчёт; нет морфологии | `W5-LANG` (язык `reason`) |
| `CONFLICT-ADJUDICATE` | частично | нет курируемого набора для оценки; в адъюдикацию уходят первые 5 кандидатов, а не лучшие | — (см. §8.4) |

Из §6: сравнения байтов вместо символов в `lib.rs` исправлены не все (осталось четыре), `README` не совпадал с дефолтной моделью, `<html lang>` не следовал локали, `index.html` кешировался — всё закрыто в волне 5.

### 8.3 Волна 5 — что сделано

Ветка `fix/wave-5-post-merge-audit`. Каждый пункт — отдельный коммит с регрессионным тестом; тест, утверждавший уязвимое поведение, развёрнут осознанно.

| ID | Задача | Сер. | Коммиты |
|---|---|---|---|
| `W5-ADMIN-TAKEOVER` | Беспарольный аккаунт никогда не получает пароль с формы логина; Google-вход не сливается с локальным аккаунтом (админ-email не исключение); адреса из `ADMIN_EMAILS` не регистрируются сами; `scripts/create_admin.py` для оператора | крит | `5fc6c22` `f55c9ab` |
| `W5-CLIENT-IP` | nginx перезаписывает `X-Forwarded-For`/`X-Real-IP` на `$remote_addr` и стирает `CF-Connecting-IP`; один `extract_client_ip` = `request.client.host` для аудита, телеметрии, `last_ip` | выс | `4c9249e` `f8045a4` |
| `W5-AUTH-THROTTLE` | Лимит логина ещё и по нормализованному email; лимит проверок `current_password` на пользователя (set-password, удаление аккаунта) | выс | `48a1cac` |
| `W5-TOKEN-VERSION` | Отозванный токен не атрибутирует активность и телеметрию и не проходит админ-гард в режиме `AUTH_DISABLED` + `ADMIN_EMAILS` | ср | `a40a319` |
| `W5-OAUTH-ERRORS` | Колбэк Google без текста исключений: редирект на `/login?error=oauth_conflict` / `oauth_failed` | ср | `ea12d4c` `a38d32e` |
| `W5-ERROR-LEAK` | Чат-стрим, ошибки джоб для владельца и админский SSE больше не отдают `str(exc)` | ср | `ad41f42` `c78a564` |
| `W5-TELEMETRY` | Только для залогиненных, с CSRF и лимитом на пользователя; белый список событий, серверные имена зарезервированы, поля ограничены; heartbeat обновляет сессию вместо строки; сессии по `(session_id, user_id)` с upsert; запись активности — не чаще раза в минуту и вне event loop; копии промптов удаляются вместе с исследованием; ретенция 90 дней | выс | `70bad92` `437fcab` `22badd7` `168ff49` `9eb2a5e` `967b343` `1c55cf6` `2142f47` `a87db4d` |
| `W5-RETRY` | `/retry` собран заново на admission (CAS из FAILED под блокировкой приёма), защищённом по статусу сбросе и примитивах джоб; LLM-лимит на роуте; requeue принимает только остановленные джобы и повышает `lease_epoch` | выс | `e8fefd2` `1e11021` |
| `W5-LEASE-COMPLETED` | Пост-коммитные действия финализации без повторной проверки аренды и без доступа к веткам отказа | выс | `f454787` |
| `W5-SEARCH-RETRY-ORDER` | Решение о повторе поиска принимается до автозапуска финализации | выс | `69a9d6e` |
| `W5-BROKER` | Воркеры в compose наконец получают `REDIS_URL`/`USE_REDIS_BROKER`/`LOG_FORMAT` (YAML-merge был поверхностным); в режиме брокера воркер добирает потерянные push из Postgres через `SKIP LOCKED` | выс | `69e42f6` `d2b6720` |
| `W5-ADMIN-MAINTENANCE` | Админские операции идут через сервисные методы (guard DEAD_LETTER, повторная отправка в брокер, сброс статусов); типизированный запрос с окном не меньше таймаута джобы | выс | `a0e7c80` `ab21a44` |
| `W5-INFRA-PORTS` | Redis, Postgres, PgBouncer, Prometheus, Loki, Grafana — только на loopback | выс | `75877f3` |
| `W5-ACCESS-LOG` | Токены шаринга вырезаются из access-логов uvicorn и nginx (они уходили в Loki) | выс | `4163ff3` |
| `W5-FE-ACCOUNT` | Удаление аккаунта шлёт `confirm`; неверный текущий пароль показывается, а не выкидывает на логин | выс | `29aaffa` `05dac78` |
| `W5-RESUME-END` | `END` в карте путей `resume_route`; `branch_stalled` и дедлайн в checkpoint | ср | `49e8052` |
| `W5-CANCEL-TRUST` | Отмена проверяется между шагами trust-suite | ср | `833d5e0` |
| `W5-TRAIL` | Атомарное добавление в `graph_trail`; SSE-курсор не застревает на лимите истории, есть `id:`; фронт дедуплицирует шаги и переподключается с backoff | ср | `1ead983` `478383c` |
| `W5-LOCKED-MERGE` | Сообщения чата добавляются под блокировкой строки; восстановление зависшей джобы патчит только флаг | ср | `3a9856f` |
| `W5-SUMMARY-LLM` | Рекомендации `/summary` считаются один раз по отпечатку задач, роут под LLM-лимитом | ср | `850ec9d` |
| `W5-ADMIN-QUERIES` | Промпты, пользователи и аналитика токенов пагинируются и сортируются в SQL, без `graph_state` целиком | ср | `07e21a2` `dc4b40a` |
| `W5-EXPORTS` | CSV отдаются потоком, ячейки-формулы нейтрализуются; экспорты с фронта через `fetch` + заголовки авторизации | ср | `5d724d4` `2f82561` `095a347` |
| `W5-ADMIN-AUDIT` | Каждая админская мутация и PII-экспорт пишут аудит и идут через админ-лимит; админа нельзя удалить из панели; тест-перечислитель роутов | ср | `ed8e2eb` `b31c9ee` |
| `W5-ADMIN-STREAM` | Админский SSE в тредпуле, переавторизация на каждом тике, здоровье считается из `get_health_summary` | ср | `c78a564` |
| `W5-METRICS` | `mas_llm_cost_usd` только с меткой `model`; e2e-тест `SEC-METRICS`; панели стоимости, алерт dead-letter на gauge, алерты на отказы воркеров и рост очереди финализации, панель логов Loki | ср | `9e5c2af` `7a57854` |
| `W5-USAGE` | Учёт LLM на каждый вызов из провайдера: фактическая модель, неокруглённая стоимость, cache-hit токены, вызовы в API-процессе | ср | `bb63e26` |
| `W5-PROVIDER-MODEL` | `DEEPSEEK_REASONER_MODEL`/`DEEPSEEK_REPAIR_MODEL` больше не подменяются дефолтной моделью | ср | `9400eac` |
| `W5-FE-ERRORS-I18N` | Сырые ошибки → локализованные сообщения; Settings, консоль агентов, прочие захардкоженные строки — ru/en/es | ср | `b29eff3` `5527180` `6261dd9` `a5cb265` |
| `W5-FE-MISC` | Явная карта источников в `MarkdownView` главнее текста; контракт ленты событий; убраны заведомо 404-ссылки; эмодзи-аватар; настройка авторазворота консоли; `<html lang>`; удалён мёртвый `telemetryApi` | низ | `bc8965f` `b167890` `79a59a8` `8483f65` `bb12e2f` `5dd9c43` `0f37a4a` |
| `W5-RUST` | Последние четыре сравнения длины в `lib.rs` считают символы; тесты паритета на кириллице (компилирует только CI) | низ | `4241c82` |
| `W5-TESTS` | Memory-нога conformance без Postgres; тесты не ходят в dev-БД, пробы Postgres падают быстро (локальный прогон — 30 с вместо 9 мин); детерминированные тай-брейки; компонентные тесты фронта | ср | `4de32c7` `c9cd935` `82eceef` `25fe7a8` `af2c4e0` `2d227a8` `a499c8e` |
| `W5-CONFORMANCE` | Conformance покрывает все 88 методов `TaskStore` на обоих бэкендах; тест-страж падает, если у метода нет теста или сигнатуры сторов расходятся с протоколом; девять расхождений in-memory с SQL исправлены; 22 теста, падавших в режиме `postgres-smoke`, починены (локальная имитация: 991 из 991); явные реэкспорты вместо `import *`; `pytest-cov` пиннут, порог покрытия 78 %; `promtool` в CI; тест конфига compose | выс | `8f30623` `742a465` `a0eec2e` `c5cc8d6` `ea67b41` `fbeb687` `0f499e3` `b942761` |
| `W5-LLM-ROUTES` | Страж: каждый роут, доходящий до LLM, зависит от `enforce_llm_rate_limit`, без исключений; `clarify`, `plan/approve` и ручной `finalize` под лимитом | ср | `69d3df7` `305df16` |
| `W5-DOCS` | README: порты, дефолтная модель, админы и CLI, ретенция, телеметрия, бюджеты; дефолтный redirect URI Google на порт web-сервиса | низ | `9b2e485` `94f0e3c` |

#### Ревью самой волны 5

Исправления тоже проверялись адверсарно: пять раундов ревью по срезам (безопасность · задачи и конкурентность · SQL и миграции · фронтенд · эксплуатация и CI · учёт LLM · язык и слой доверия), каждая находка — отдельный скептик с воспроизведением. Для SQL поднимался временный PostgreSQL 16, и имитация CI-джобы `postgres-smoke` гонялась локально. Число подтверждённых находок по раундам: 35 (29 плюс 6 воспроизведённых пробами) → 19 → 10 → 5 → 1. Каждый раунд проверял уже исправленный код, поэтому часть находок — дефекты самих исправлений (квадратичная регулярка редактирования, заглушка задачи, обходившая правило «все задачи упали»). Итоговая проверка на временном PostgreSQL 16: имитация `postgres-smoke` — 1615 тестов без падений, миграции проходят вниз до `000027` и обратно без расхождений схемы. Главное из исправленного:

| ID | Что | Сер. | Коммиты |
|---|---|---|---|
| `W5-R-XSS` | Подмена `[Sn]` в `MarkdownView` шла регуляркой по готовому HTML и залезала в атрибуты (`alt`, `title`) — хранимая XSS из текста отчёта, доходящая до публичных `/r/`-страниц. Цитаты переписываются только в тексте, ссылки — только `http(s)`. Отдельно nginx теперь отдаёт CSP, `frame-ancestors`, `nosniff` и `Referrer-Policy` (`no-referrer` на `/r/`) | выс | `11e5259` `2b5187d` `61197c8` |
| `W5-R-ADMIN-IDENTITY` | Аккаунт, самостоятельно зарегистрированный до того, как его адрес добавили в `ADMIN_EMAILS`, становился админом. Теперь права дают только привязанная Google-личность или провижининг через `scripts/create_admin.py` (`users.admin_provisioned_at`) | выс | `3448f06` |
| `W5-R-SECRET` | С `AUTH_DISABLED=true` и заданными `ADMIN_EMAILS` принимался админский токен, подписанный публичным дефолтным секретом; старт теперь отказывает | выс | `463b832` |
| `W5-R-SESSIONS` | Logout отзывает все сессии аккаунта; первый пароль Google-аккаунта и сброс без текущего пароля требуют свежего (≤10 мин) входа через Google; OAuth-state токены не проходят как сессионные | ср | `df38005` `5bdd945` `8a76e0d` `3561de4` `a33db08` |
| `W5-R-SURFACE` | `/health/workers/*` и `/health/queues` только для админа (отдавали сырые ошибки БД); `PATCH /v1/tasks` только для админа (владелец мог подменить собранное содержимое источников опубликованного отчёта); SSRF отсекает все неглобальные адреса, включая CGNAT; CSRF на GET-экспортах; валидация `X-Request-ID`; URL вебхуков в логах — только хост; лимитеры с вытеснением ключей | ср | `240aafe` `0f4a77b` `0567ec5` `810fe0c` `04d425f` `7a414d3` `2c10b68` `dc427fd` `b1dd071` |
| `W5-R-REDACTION` | Токены шаринга в логах при нестандартном написании пути; `hide_parameters` у движков SQLAlchemy; нейтрализация формул в CSV за пробелами и полноширинными знаками | ср | `d47ff2d` `2cac197` |
| `W5-R-JOBS` | Retry больше не оставляет исследование в PROCESSING без работы, перестраивает план после «не удалось построить план»; admin-requeue устаревшей dead-letter-джобы не откатывает завершённое исследование; восстановление зависших джоб не воскрешает отменённое; окно «FAILED считается завершённым» закрыто; sweep застрявших исследований | ср | `ec3004f` `39e850d` `7212cb4` `9959167` `a4effec` `db59ddf` `60f8572` `9af35e2` `fee4b7c` |
| `W5-R-DATA` | Миграция `000028` сама чинит INVALID-индекс; страница пользователей агрегирует только свою страницу; ретенция и экспорт промптов батчами и по keyset; строки учёта LLM не теряются при удалении владельца и пишутся вне слота; осиротевшие копии промптов удаляются одноразовой миграцией `000032` | ср | `207084b` `554cbcc` `3ee76f0` `d5624c4` `ba1a0a4` `e34c474` `4e573cd` `828e4b8` |
| `W5-R-METRICS` | `deepseek-reasoner` тарифицировался по ставке pro при фактической flash; метка HTTP-метода не ограничена; отказы джоб считались как `success` (алерт на отказы не мог сработать); панели стоимости смешивали многопроцессный счётчик API | ср | `1d2c8e3` `e3b6b78` `245eb6f` `97dbadc` `b7bda07` `6680cfa` |
| `W5-R-TRUST` | Стабильные id источников мини-поиска в чате между ходами; исключение «иностранного» источника в аудите цитат только при уверенном определении языка; немецкие существительные больше не считаются именами; задачи чата не портят покрытие плана; однострочные причины конфликтов; испанские структурные заголовки | ср | `6fec569` `f2be90f` `4f4fede` `f08dba5` `eef9449` `b01294b` `fc43c75` |
| `W5-R-LATE` | Раунды 3–5: регулярка редактирования токенов стала линейной (была квадратичной — один запрос блокировал event loop почти на секунду); CSRF для админских cookie-сессий в режиме `AUTH_DISABLED`; лимитеры не забывают заблокированный ключ; нейтрализация формул после `;` и переводов строк; паритет сторов по результатам задач (ломал `postgres-smoke`); восстановление потерянных декомпозиций вне 50 последних исследований; задачи плана без запросов закрываются, а правило «все задачи упали» считается только по задачам с запросами; CAS финализации перепроверяет незавершённые поиски; requeue поиска только для активного исследования; таблицы с цитатами в режиме проверки; возврат после неудачного повторного входа через Google | ср | `7cec18d` `d1d5274` `edbd532` `2e02cba` `514f0a2` `9d94ca4` `73d88b4` `377c40c` `df7c4ca` `7daf17f` `a55eec9` `ea5bf14` `2abdb8e` `cd38001` `6a275b2` `22ba331` `33cca74` |

### 8.4 Что осталось после волны 5

Сознательно не взято в волну или найдено по ходу; каждое — кандидат в следующий план.

| Что | Почему важно | Где |
|---|---|---|
| Сбой поискового провайдера неотличим от пустой выдачи | Транзиентный отказ Tavily/DuckDuckGo даёт `COMPLETED` с нулём источников и не ретраится; `ddgs` бросает одно и то же исключение на «ничего не найдено» и на отказ | `src/providers/search.py` |
| `enqueue_research_finalization` не атомарен | CAS статуса и вставка джобы — разные транзакции; падение между ними оставляет `ANALYZING` без джобы | `research_service.py` |
| Цифра стоимости в UI — из счётчика процесса | `graph_state.llm_token_usage` пишется при финализации из общего счётчика провайдера и может смешивать параллельные вызовы; точный источник теперь `llm_usage_logs` | `research_service.py`, `/v1/auth/token-stats` |
| Нет ретенции `llm_usage_logs`; админская сводка — ~14 полных агрегатов каждые 6 с; `ILIKE` по `details->>'prompt'` без `pg_trgm` | Растёт линейно с каждым LLM-вызовом (десятки строк на исследование) | `sqlalchemy_task_store.py` |
| `CONFLICT-ADJUDICATE`: нет курируемого набора; в адъюдикацию уходят первые 5 кандидатов, без заголовка и даты источника | Критерий волны 3 «отсекает большинство ложных» не измерен | `analyzer.py`, `eval/` |
| `ACQ-PDF`: лимит размера проверяется после загрузки всего тела; 5 МБ режут многие отчёты регуляторов; парсится до 1 млн символов при использовании 10 тыс. | Память и время воркера | `net_safety.py`, `providers/search.py` |
| Метрики API при `API_WORKERS>1` — по процессам, без multiprocess-режима; нет Alertmanager; `METRICS_TOKEN` требует ручной правки `prometheus.yml` | Алерты видны только в UI Prometheus, счётчики «скачут» между процессами | `ops/`, `docker-compose.yml` |
| Redis без пароля; Grafana и Postgres с дефолтными кредами | После `W5-INFRA-PORTS` всё на loopback, но compose не требует смены паролей | `docker-compose.yml` |
| Токен шаринга может попасть в `error_log` nginx | `log_format` не распространяется на error log | `web/nginx.conf` |
| Учение по восстановлению из бэкапа не зафиксировано | Критерий волны 2 | `README.md` |
| Порог покрытия 78 % измерен на Windows (79,5 %) | Если на Linux меньше, джоба упадёт на пороге | `pyproject.toml` |
| Постоянный лимит логина по аккаунту можно использовать для блокировки чужого входа по паролю | Цена защиты от распределённого подбора; Google-вход не блокируется | `src/auth/login_rate_limit.py` |
| Нет подтверждения email | Неподтверждённая локальная регистрация чужого адреса блокирует его последующий Google-вход (`oauth_conflict`); помогает только удаление аккаунта оператором | `auth_mixin.py` (см. §8.5 п. 4) |
| Grafana не показывает LLM-траты API-процесса | Панели стоимости читают только воркеры: многопроцессный счётчик API без multiprocess-режима неаддитивен; точные цифры — во вкладке аналитики админки | `ops/grafana/` |
| CSV: `;` внутри ячейки и префиксы `\|` и `%0A` | В Excel с разделителем `;` ячейка вида `x;=1+1` станет формулой; полноценное решение меняет формат экспорта | `src/api/app.py` |
| Переход в FAILED «все задачи упали» без защитной проверки | Последняя находка ревью (низкая): одновременный admin-requeue поиска может получить 200, а исследование всё равно уйдёт в FAILED с задачей в PENDING. Нужен защищённый переход статуса в сторе по тому же правилу, что и CAS финализации | `research_service.py` `_get_research_for_finalization` |
| Строгий детектор языка в аудите цитат | Короткий источник на том же алфавите, что и отчёт, теперь считается «своим» и может быть помечен как неподкреплённый; японский с долей каны < 10 % определяется как китайский | `citation_audit.py`, `cross_language.py` |
| CSP проверена в браузере не на всех экранах | Chrome: логин, главная, публичная ссылка; исследование с SSE, Settings и `/admin` — только чтением кода | `web/nginx.conf` |

### 8.5 Критика и предложения

**1. Гейт есть, но он не обязателен.** Волна 1 сделала CI настоящим, а `feature/admin-panel` всё равно влит в main с красным Quality Gates, и починка ушла прямыми коммитами в main (`58d34aa`, `0f05953`) — ни один из них не проходил ревью. Красный гейт жил минуты, но критическая уязвимость и мёртвый `/retry` прошли в main именно этой дорогой. Нужны branch protection на `main` с обязательными `Quality Gates` и `Postgres Smoke`, запрет прямого пуша и мерж только через PR. Это настройка репозитория, код тут не поможет.

**2. Классы дефектов, а не отдельные баги.** Волна 5 — почти целиком повтор классов из §5 в новом коде: утечка `str(exc)`, запись без блокировки, sync-I/O на event loop, вход в тратящее состояние мимо admission, отсутствие ретенции. Договорённости не масштабируются, стражи масштабируются. В этой волне добавлены тесты-перечислители: LLM-лимит на каждом LLM-роуте, аудит и лимит на каждой админской мутации, conformance для каждого метода стора, явный экспорт домена, конфиг compose. Стоит довести набор ещё двумя AST-стражами: `async`-роут или middleware не вызывает синхронный стор напрямую, и все записи `graph_state` идут через merge-примитивы. Плюс PR-шаблон с чек-листом этих классов.

**3. Тайпчекер окупился бы на одном `/retry`.** Роут отдавал 500 на каждый вызов из-за трёх ошибок, каждую из которых ловит pyright в режиме basic: несуществующий член enum, несуществующий метод протокола, перепутанный порядок аргументов. Предложение: pyright в CI на `src/services` и `src/repositories` (там `Protocol` уже есть), затем на остальной `src/`.

**4. Отсутствие подтверждения email — корень целого семейства атак.** Строгая модель закрыла админов, но обычный пользователь по-прежнему уязвим к предварительной регистрации. Злоумышленник регистрирует чужой адрес, и настоящий владелец потом не может войти через Google (409 `oauth_conflict`). Корректное решение — подтверждение email при регистрации и безопасное связывание Google с подтверждённым локальным аккаунтом. Оно же разблокирует пользователей, созданных до миграции `000019` (см. §8.6).

**5. Продуктовая аналитика живёт в основной БД.** `user_events`, `user_sessions`, `llm_usage_logs` и админская сводка с полными агрегатами каждые 6 секунд делят Postgres с исследованиями. Пока объёмы малы, это нормально. При росте нужны дневные rollup-таблицы или вынос событий в отдельное хранилище, иначе админка станет самым тяжёлым потребителем БД.

**6. Совет §6 про CSP оказался неверным.** План считал отсутствие CSP несрочным, потому что «единственный сток `v-html` кормится из `MarkdownIt({ html: false })`». Ревью волны 5 нашло хранимую XSS именно в этом стоке: постобработка цитат работала поверх уже отрендеренного HTML. Эшелонирование нужно именно потому, что «известного пути инъекции нет» — утверждение о текущем знании, а не о коде. CSP теперь есть.

**7. Ревью исправлений окупилось.** Каждый раунд правок порождал новые дефекты: первое ревью волны 5 подтвердило 29 находок плюс 6 воспроизведённых пробами, второе — ещё 19, в том числе две уязвимости уровня «админ без пароля». Имеет смысл сделать адверсарное ревью диффа и прогон `postgres-smoke` обязательной частью каждого крупного PR, а не разовой акцией.

**8. Админский слой — кандидат на выделение.** `sqlalchemy_task_store.py` (2 700 строк), `in_memory_task_store.py` (1 900) и `app.py` (1 650) выросли в основном за счёт админки и телеметрии. Теперь, когда conformance покрывает каждый метод, безопасно вынести админские чтения в отдельный `AdminStore`, а роуты — в отдельный `APIRouter`. §6 предостерегал от рефакторинга без сьюта, и это предусловие теперь выполнено.

### 8.6 Решения за владельцем

| Вопрос | Варианты |
|---|---|
| **Личный адрес в истории git** | Адрес оператора, захардкоженный в `tests/test_admin_authz.py` коммитом `2120223`, убран из текущих файлов, но остаётся в опубликованной истории main. Либо принять, либо переписать историю (`git filter-repo`), что потребует force-push и переклонирования у всех участников; решать до мержа этой ветки. |
| **Пользователи Google, созданные до `20260904_000019`** | У них не сохранён `google_subject` и случайный пароль, поэтому при строгой модели Google-вход даёт `oauth_conflict`, а входа по паролю нет. Варианты: контролируемое связывание при `email_verified` от Google со сбросом пароля и отзывом сессий, поток восстановления по email или ручное восстановление оператором. |
| **Перед мержем** | `postgres-smoke` и `Quality Gates` должны пройти в GitHub: SQL-пути, миграции `000028`–`000032`, Rust-тесты и шаг `promtool` проверены локально (временный PostgreSQL 16 и promtool), но в CI ещё не запускались. |
| **Шаг при обновлении** | Админы, заведённые паролем до этой ветки, теряют права, пока оператор один раз не выполнит для них `scripts/create_admin.py` (он же сбрасывает пароль и сессии). Миграция `000032` необратимо удаляет осиротевшие копии промптов. |
