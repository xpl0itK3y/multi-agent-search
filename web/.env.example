# Backend base URL. Leave empty to use the Vite dev-proxy (recommended in dev).
# VITE_API_BASE=http://localhost:8000

# Override the dev-proxy target if the backend runs elsewhere.
# VITE_API_PROXY=http://localhost:8000

# Display name in the greeting / user card (until auth lands).
VITE_USER_NAME=denis
